cleaner = require("../grbl-gcode-cleaner.js");

var ok = function(a, msg) { if (!a) throw new Error(msg || "not ok"); };
var eq = function(a, b) { if (a!==b) throw new Error(a + " !== " + b); };

describe('grbl-gcode-cleaner', function() {
  describe('#', function() {
    it('', function() {

    });
  });
});
